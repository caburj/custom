<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

<t t-name="ai_debug.ImportPreviewDialog">
    <Dialog title="'Import Traces'" size="'sm'">
        <t t-set-slot="default">
            <t t-if="this.props.errorMessage">
                <p class="text-danger mb-0" t-out="this.props.errorMessage"/>
            </t>
            <t t-else="">
                <p class="mb-0">
                    <strong t-out="this.props.traceCount"/> trace(s) found.
                    <t t-if="this.props.duplicateCount > 0">
                        <br/><strong t-out="this.props.duplicateCount"/> duplicate(s) will be overwritten.
                    </t>
                </p>
            </t>
        </t>
        <t t-set-slot="footer">
            <t t-if="!this.props.errorMessage">
                <button class="btn btn-primary"
                        t-on-click="() => { this.props.onConfirm(); this.props.close(); }">
                    Import
                </button>
            </t>
            <button class="btn btn-secondary" t-on-click="this.props.close">Cancel</button>
        </t>
    </Dialog>
</t>

</templates>
