---
phase: 01-data-models-and-instrumentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai_debug/__manifest__.py
  - ai_debug/__init__.py
  - ai_debug/models/__init__.py
  - ai_debug/models/ai_debug_trace.py
  - ai_debug/models/ai_debug_iteration.py
  - ai_debug/models/ai_debug_tool_call.py
  - ai_debug/security/ir.model.access.csv
autonomous: true
requirements:
  - CAPT-01
  - CAPT-02
  - CAPT-03
  - CAPT-04
  - CAPT-05
  - CAPT-06
  - CAPT-07
  - CAPT-10
  - CAPT-11
  - CONF-02

must_haves:
  truths:
    - "ai_debug module can be installed on an Odoo instance that has the ai module"
    - "ai.debug.trace, ai.debug.iteration, ai.debug.tool.call tables exist in the database after install"
    - "Only admin users (base.group_system) can access debug records"
    - "Autovacuum method deletes traces older than configurable retention period"
  artifacts:
    - path: "ai_debug/__manifest__.py"
      provides: "Module declaration with depends=['ai']"
      contains: "'depends': ['ai']"
    - path: "ai_debug/models/ai_debug_trace.py"
      provides: "Trace model with all capture fields and autovacuum"
      contains: "class AiDebugTrace"
    - path: "ai_debug/models/ai_debug_iteration.py"
      provides: "Iteration model with messages_sent, raw_response Json fields"
      contains: "class AiDebugIteration"
    - path: "ai_debug/models/ai_debug_tool_call.py"
      provides: "Tool call model with args, result, state snapshot fields"
      contains: "class AiDebugToolCall"
    - path: "ai_debug/security/ir.model.access.csv"
      provides: "Admin-only access rules for all three models"
      contains: "base.group_system"
  key_links:
    - from: "ai_debug/models/ai_debug_iteration.py"
      to: "ai_debug/models/ai_debug_trace.py"
      via: "trace_id Many2one with ondelete='cascade'"
      pattern: "trace_id.*Many2one.*ai\\.debug\\.trace"
    - from: "ai_debug/models/ai_debug_tool_call.py"
      to: "ai_debug/models/ai_debug_iteration.py"
      via: "iteration_id Many2one with ondelete='cascade'"
      pattern: "iteration_id.*Many2one.*ai\\.debug\\.iteration"
    - from: "ai_debug/models/ai_debug_trace.py"
      to: "ir.config_parameter"
      via: "@api.autovacuum reading ai_debugger.retention_days"
      pattern: "autovacuum.*retention_days"
---

<objective>
Create the ai_debug Odoo module scaffold and define three persistent data models (ai.debug.trace, ai.debug.iteration, ai.debug.tool.call) with all fields needed for full agentic loop observability, plus admin-only security rules and autovacuum retention cleanup.

Purpose: Establish the storage schema that the instrumentation layer (Plan 02) will populate. Without these models and their fields, no capture data can be persisted.
Output: Installable Odoo module with three ORM models, security CSV, and autovacuum method.
</objective>

<execution_context>
@/Users/joseph/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joseph/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-models-and-instrumentation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create module scaffold (manifest, init files)</name>
  <files>ai_debug/__manifest__.py, ai_debug/__init__.py, ai_debug/models/__init__.py</files>
  <action>
Create the `ai_debug/` directory structure and three boilerplate files:

1. **`ai_debug/__manifest__.py`**: Module manifest with these exact values:
   - `'name': 'AI Debug'`
   - `'version': '1.0'`
   - `'category': 'Technical'`
   - `'summary': 'Instrument the AI agentic loop for full observability'`
   - `'depends': ['ai']`
   - `'data': ['security/ir.model.access.csv']`
   - `'installable': True`
   - `'application': False` (no Apps grid tile — per locked decision)
   - `'auto_install': False`
   - `'license': 'LGPL-3'`

2. **`ai_debug/__init__.py`**: Single line importing models subpackage: `from . import models`

3. **`ai_debug/models/__init__.py`**: Import the three model modules (NOT ai_session — that comes in Plan 02):
   ```python
   from . import ai_debug_trace
   from . import ai_debug_iteration
   from . import ai_debug_tool_call
   ```

Per locked decision: technical name is `ai_debug`, display name is "AI Debug", category is Technical, not an application.
  </action>
  <verify>All three files exist with correct Python syntax: `python3 -c "import ast; [ast.parse(open(f).read()) for f in ['ai_debug/__manifest__.py', 'ai_debug/__init__.py', 'ai_debug/models/__init__.py']]"`</verify>
  <done>Module scaffold exists with manifest declaring depends=['ai'], application=False, and models __init__ importing the three model modules.</done>
</task>

<task type="auto">
  <name>Task 2: Define data models, security CSV, and autovacuum</name>
  <files>ai_debug/models/ai_debug_trace.py, ai_debug/models/ai_debug_iteration.py, ai_debug/models/ai_debug_tool_call.py, ai_debug/security/ir.model.access.csv</files>
  <action>
Create the three model files and security CSV. Use the field definitions from 01-RESEARCH.md Code Examples section as the reference. All JSON data MUST use `fields.Json` (not Text) per locked decision.

**`ai_debug/models/ai_debug_trace.py`** — `ai.debug.trace` model:
- `_name = 'ai.debug.trace'`, `_description = 'AI Debug Trace'`, `_order = 'create_date desc, id desc'`
- Fields:
  - `agent_id`: Many2one to `'ai.agent'`, string='Agent', ondelete='set null', index=True (nullable — `_run_agentic_loop` is `@api.model`, may not have agent context)
  - `llm_model`: Char, string='LLM Model', index=True
  - `instructions`: Text, string='System Instructions' (CAPT-09 storage)
  - `rag_context`: Text, string='RAG Context' (CAPT-09 storage)
  - `state`: Selection with values `[('running', 'Running'), ('done', 'Done'), ('error', 'Error'), ('paused', 'Awaiting Confirmation')]`, default='running', index=True, required=True (CAPT-06, CAPT-08)
  - `termination_reason`: Char, string='Termination Reason' (CAPT-06)
  - `error_message`: Text, string='Error Message' (CAPT-08)
  - `start_time`: Datetime, string='Started', default=fields.Datetime.now
  - `total_duration_ms`: Integer, string='Duration (ms)' (CAPT-07)
  - `iteration_count`: Integer, string='Iterations' (CAPT-01)
  - `iteration_ids`: One2many to `'ai.debug.iteration'`, inverse_name='trace_id', string='Iterations'
- Method `_gc_ai_debug_traces` decorated with `@api.autovacuum`:
  - Read retention days from `ir.config_parameter` key `ai_debugger.retention_days` with default `"7"`
  - Calculate cutoff datetime using `fields.Datetime.subtract(fields.Datetime.now(), days=retention_days)`
  - `self.search([('create_date', '<=', cutoff)]).unlink()`
  - This fulfills CONF-02

**`ai_debug/models/ai_debug_iteration.py`** — `ai.debug.iteration` model:
- `_name = 'ai.debug.iteration'`, `_description = 'AI Debug Iteration'`, `_order = 'index asc'`
- Fields:
  - `trace_id`: Many2one to `'ai.debug.trace'`, required=True, ondelete='cascade', index=True
  - `index`: Integer, string='Index', required=True (0-based iteration number)
  - `messages_sent`: Json, string='Messages Sent' (CAPT-04 — verbatim, no truncation)
  - `raw_response`: Json, string='Raw Provider Response' (CAPT-05 — verbatim)
  - `state_before`: Json, string='State Before' (iteration-level state snapshot)
  - `state_after`: Json, string='State After'
  - `final_message`: Json, string='Final Message'
  - `duration_ms`: Integer, string='Duration (ms)' (CAPT-07)
  - `tool_call_ids`: One2many to `'ai.debug.tool.call'`, inverse_name='iteration_id', string='Tool Calls'

**`ai_debug/models/ai_debug_tool_call.py`** — `ai.debug.tool.call` model:
- `_name = 'ai.debug.tool.call'`, `_description = 'AI Debug Tool Call'`, `_order = 'id asc'`
- Fields:
  - `iteration_id`: Many2one to `'ai.debug.iteration'`, required=True, ondelete='cascade', index=True
  - `tool_name`: Char, string='Tool Name', index=True
  - `call_id`: Char, string='Call ID'
  - `args`: Json, string='Arguments' (CAPT-03 — verbatim per locked decision)
  - `result`: Text, string='Result' (Text not Json — result may be plain string)
  - `success`: Boolean, string='Success', default=True
  - `triggered_confirmation`: Boolean, string='Required Confirmation' (CAPT-10)
  - `confirmation_message`: Text, string='Confirmation Message' (CAPT-10)
  - `state_before`: Json, string='State Before' (CAPT-11 — per tool call per locked decision)
  - `state_after`: Json, string='State After' (CAPT-11)
  - `duration_ms`: Integer, string='Duration (ms)' (CAPT-07)

**`ai_debug/security/ir.model.access.csv`** — Admin-only access (per locked decision: `base.group_system`):
```csv
id,name,model_id:id,group_id:id,perm_read,perm_write,perm_create,perm_unlink
access_ai_debug_trace_system,ai.debug.trace admin,model_ai_debug_trace,base.group_system,1,1,1,1
access_ai_debug_iteration_system,ai.debug.iteration admin,model_ai_debug_iteration,base.group_system,1,1,1,1
access_ai_debug_tool_call_system,ai.debug.tool.call admin,model_ai_debug_tool_call,base.group_system,1,1,1,1
```
  </action>
  <verify>
1. Python syntax check: `python3 -c "import ast; [ast.parse(open(f).read()) for f in ['ai_debug/models/ai_debug_trace.py', 'ai_debug/models/ai_debug_iteration.py', 'ai_debug/models/ai_debug_tool_call.py']]"`
2. Verify all required fields exist by grepping: `grep -c 'fields\.' ai_debug/models/ai_debug_trace.py` (expect 11+), `grep -c 'fields\.' ai_debug/models/ai_debug_iteration.py` (expect 8+), `grep -c 'fields\.' ai_debug/models/ai_debug_tool_call.py` (expect 10+)
3. Verify Json fields used (not Text) for JSON data: `grep 'fields.Json' ai_debug/models/*.py` (expect matches for messages_sent, raw_response, args, state_before, state_after)
4. Verify autovacuum: `grep 'autovacuum' ai_debug/models/ai_debug_trace.py` (expect match)
5. Verify security CSV has 3 data lines: `wc -l ai_debug/security/ir.model.access.csv` (expect 4 — header + 3 entries)
  </verify>
  <done>Three model files define all fields needed for CAPT-01 through CAPT-11 storage. Security CSV grants admin-only (base.group_system) CRUD access. Autovacuum method on trace model reads retention_days config param and deletes old traces (CONF-02).</done>
</task>

</tasks>

<verification>
1. Module structure is valid: `__manifest__.py` has correct `depends`, `data`, `application=False`
2. All three models define `_name`, `_description`, `_order`
3. Relational chain: trace -> iteration (One2many/Many2one cascade) -> tool_call (One2many/Many2one cascade)
4. All JSON payload fields use `fields.Json` (not Text): messages_sent, raw_response, args, state_before, state_after, final_message
5. `@api.autovacuum` method exists on trace model reading `ai_debugger.retention_days`
6. Security CSV has exactly 3 access rules for `base.group_system`
7. No menu items or views — those are Phase 2 scope
</verification>

<success_criteria>
- The ai_debug directory is a valid Odoo module directory with __manifest__.py, __init__.py, models/, and security/
- All field types match locked decisions (Json for JSON data, Selection for state)
- Cascade deletion flows from trace -> iteration -> tool_call
- Admin-only access enforced via security CSV
- Autovacuum configured with configurable retention
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-models-and-instrumentation/01-01-SUMMARY.md`
</output>
