---
phase: 18-display-components-and-animation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai_debug/static/src/app/format_metrics.js
  - ai_debug/static/src/app/app.js
  - ai_debug/static/src/app/app.xml
  - ai_debug/static/src/app/app.scss
  - ai_debug/static/src/app/detail/iter_detail.js
  - ai_debug/static/src/app/detail/iter_detail.xml
autonomous: true
requirements: [SIDE-01, DETL-01]

must_haves:
  truths:
    - "Sidebar trace rows show compact metrics line with time and input/output token split"
    - "IterationDetail header shows duration and token count chips"
    - "Metrics line hidden for traces with zero token and duration data (pre-Phase-17 records)"
    - "Token counts use smart abbreviation (exact < 1000, Xk >= 1000, XM >= 1M)"
    - "Duration uses adaptive units (Xms < 1s, Xs < 60s, Xm Xs >= 60s)"
  artifacts:
    - path: "ai_debug/static/src/app/format_metrics.js"
      provides: "Shared formatting utilities"
      exports: ["formatTokens", "formatDuration"]
    - path: "ai_debug/static/src/app/app.xml"
      provides: "Sidebar metrics line in trace rows"
      contains: "ai-tree-metrics-line"
    - path: "ai_debug/static/src/app/detail/iter_detail.xml"
      provides: "Token and duration chips in IterationDetail header"
      contains: "ai-metric-chip"
  key_links:
    - from: "ai_debug/static/src/app/app.js"
      to: "ai_debug/static/src/app/format_metrics.js"
      via: "import { formatTokens, formatDuration }"
      pattern: "import.*formatTokens.*format_metrics"
    - from: "ai_debug/static/src/app/app.xml"
      to: "getTraceTotals in app.js"
      via: "t-set totals t-value this.getTraceTotals(node.trace)"
      pattern: "getTraceTotals\\(node\\.trace\\)"
    - from: "ai_debug/static/src/app/detail/iter_detail.js"
      to: "ai_debug/static/src/app/format_metrics.js"
      via: "import { formatTokens, formatDuration }"
      pattern: "import.*formatTokens.*format_metrics"
---

<objective>
Create shared formatting utilities, add sidebar metrics line to trace rows, and add token/duration chips to IterationDetail header.

Purpose: Give developers at-a-glance time and token metrics in the sidebar and iteration-level detail view, with smart number abbreviation and adaptive time units.
Output: format_metrics.js utility, sidebar metrics line in trace rows, IterationDetail header chips with SCSS styles.
</objective>

<execution_context>
@/Users/joseph/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joseph/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-display-components-and-animation/18-RESEARCH.md
@.planning/phases/17-frontend-reactive-store-and-idb-persistence/17-01-SUMMARY.md
@ai_debug/static/src/app/app.js
@ai_debug/static/src/app/app.xml
@ai_debug/static/src/app/app.scss
@ai_debug/static/src/app/detail/iter_detail.js
@ai_debug/static/src/app/detail/iter_detail.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create format_metrics.js utility and add sidebar metrics line</name>
  <files>
    ai_debug/static/src/app/format_metrics.js
    ai_debug/static/src/app/app.js
    ai_debug/static/src/app/app.xml
    ai_debug/static/src/app/app.scss
  </files>
  <action>
**1. Create `format_metrics.js`** — new file with two pure exported functions:

```javascript
/** @odoo-module **/

/**
 * Format a token count with smart abbreviation.
 * < 1000: exact number (e.g. "450")
 * >= 1000: one decimal "k" (e.g. "3.4k")
 * >= 1000000: one decimal "M" (e.g. "1.2M")
 * Falsy/0 returns "0".
 */
export function formatTokens(n) {
    if (!n) return "0";
    if (n >= 1_000_000) return `${(n / 1_000_000).toFixed(1)}M`;
    if (n >= 1_000) return `${(n / 1_000).toFixed(1)}k`;
    return String(n);
}

/**
 * Format a duration in milliseconds with adaptive units.
 * null/undefined: "–"
 * < 1000ms: "850ms"
 * < 60000ms: "1.2s"
 * >= 60000ms: "2m 14s"
 */
export function formatDuration(ms) {
    if (!ms && ms !== 0) return "\u2013";
    if (ms < 1000) return `${Math.round(ms)}ms`;
    if (ms < 60000) return `${(ms / 1000).toFixed(1)}s`;
    const mins = Math.floor(ms / 60000);
    const secs = Math.round((ms % 60000) / 1000);
    return `${mins}m ${secs}s`;
}
```

Note: `_formatDuration` already exists in `AiDebugApp` (lines 791-797) with identical logic. After creating the shared module, update `_formatDuration` to delegate to the imported `formatDuration` — or simply keep it as-is and bind the new ones separately. The simplest approach: keep `_formatDuration` for existing callers (`getIterationDuration`), and bind the new functions for template use. Do NOT break existing iteration duration display.

**2. In `app.js`:**
- Add import: `import { formatTokens, formatDuration } from "./format_metrics";`
- In setup(), bind for template use: `this.formatTokens = formatTokens;` and `this.formatDuration = formatDuration;`
- `getTraceTotals(trace)` already exists and is reactive — no changes needed to it.

**3. In `app.xml`:**
- Inside the trace row label (the `<span class="ai-tree-label">` block for `node.type === 'trace'`), AFTER the existing `<span class="ai-tree-meta-line">` (agent + model line), add a NEW metrics line:

```xml
<t t-set="totals" t-value="this.getTraceTotals(node.trace)"/>
<span class="ai-tree-metrics-line"
      t-if="totals.total_duration_ms > 0 or totals.total_tokens > 0">
    <t t-esc="this.formatDuration(totals.total_duration_ms)"/>
    <t t-if="totals.total_input > 0 and totals.total_output > 0">
        &#xB7; <t t-esc="this.formatTokens(totals.total_input)"/>&#x2192;<t t-esc="this.formatTokens(totals.total_output)"/> tok
    </t>
    <t t-elif="totals.total_tokens > 0">
        &#xB7; <t t-esc="this.formatTokens(totals.total_tokens)"/> tok
    </t>
</span>
```

This follows the locked decision: middle dot separator, input→output split, adaptive time, smart token abbreviation. The `t-if` gate ensures pre-Phase-17 traces with zero data don't show "0ms · 0→0 tok". When only total is available (input+output both zero but total > 0), it falls back to showing total only.

**4. In `app.scss`:**
- Add `.ai-tree-metrics-line` style AFTER the existing `.ai-tree-meta-line` block:

```scss
.ai-tree-metrics-line {
    display: block;
    font-size: 10px;
    color: $o-gray-500;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    line-height: 1.3;
}
```

Per locked decision: secondary text, smaller font, muted color below trace name. Use `display: block` so it falls on its own line within the `.ai-tree-label` span.
  </action>
  <verify>
    <automated>cd /Users/joseph/clones/odoo/custom/.worktrees/master-ai-sub-agents-dpro &amp;&amp; test -f ai_debug/static/src/app/format_metrics.js &amp;&amp; grep -q "formatTokens" ai_debug/static/src/app/format_metrics.js &amp;&amp; grep -q "formatDuration" ai_debug/static/src/app/format_metrics.js &amp;&amp; grep -q "import.*formatTokens.*format_metrics" ai_debug/static/src/app/app.js &amp;&amp; grep -q "ai-tree-metrics-line" ai_debug/static/src/app/app.xml &amp;&amp; grep -q "ai-tree-metrics-line" ai_debug/static/src/app/app.scss &amp;&amp; echo "PASS"</automated>
    <manual>Run Odoo dev server, trigger an agentic loop, verify sidebar trace rows show a compact metrics line like "1.2s · 1.2k→2.2k tok" below the agent/model line. Verify it updates as new iterations arrive. Verify old hydrated traces without metrics don't show the line.</manual>
  </verify>
  <done>format_metrics.js exists with formatTokens and formatDuration exports. Sidebar trace rows display a metrics line with adaptive time and smart token abbreviation that updates live via OWL reactivity. Pre-Phase-17 traces with zero data show no metrics line.</done>
</task>

<task type="auto">
  <name>Task 2: Add token and duration chips to IterationDetail header</name>
  <files>
    ai_debug/static/src/app/detail/iter_detail.js
    ai_debug/static/src/app/detail/iter_detail.xml
    ai_debug/static/src/app/app.scss
  </files>
  <action>
**1. In `iter_detail.js`:**
- Add import: `import { formatTokens, formatDuration } from "../format_metrics";`
- In `setup()`, bind for template use: `this.formatTokens = formatTokens;` and `this.formatDuration = formatDuration;`

**2. In `iter_detail.xml`:**
- In the `.ai-detail-header` div, AFTER the existing "Error" meta span (`<span t-if="props.iteration.has_error" ...>`), add duration and token chips:

```xml
<span t-if="props.iteration.duration_ms"
      class="ai-metric-chip">
    <t t-esc="this.formatDuration(props.iteration.duration_ms)"/>
</span>
<span t-if="props.iteration.tokens and props.iteration.tokens.total > 0"
      class="ai-metric-chip">
    <t t-esc="this.formatTokens(props.iteration.tokens.total)"/> tok
</span>
```

Per Claude's discretion: use monochrome chips (single neutral color), not color-coded by metric type. This keeps the header clean and matches the data-dense developer-tool aesthetic.

**3. In `app.scss`:**
- Add `.ai-metric-chip` style (used by both IterationDetail and LoopDetail in Plan 02):

```scss
.ai-metric-chip {
    display: inline-flex;
    align-items: center;
    padding: 2px 8px;
    border-radius: 10px;
    font-size: 11px;
    font-weight: 500;
    background-color: $o-gray-200;
    color: $o-gray-700;
    margin-left: 6px;
}
```

Use Odoo SCSS variables only — no hardcoded hex values. The chip style will also be reused by the live timer chip in Plan 02 (adding `--live` modifier there).
  </action>
  <verify>
    <automated>cd /Users/joseph/clones/odoo/custom/.worktrees/master-ai-sub-agents-dpro &amp;&amp; grep -q "import.*formatTokens.*format_metrics" ai_debug/static/src/app/detail/iter_detail.js &amp;&amp; grep -q "ai-metric-chip" ai_debug/static/src/app/detail/iter_detail.xml &amp;&amp; grep -q "ai-metric-chip" ai_debug/static/src/app/app.scss &amp;&amp; echo "PASS"</automated>
    <manual>Run Odoo dev server, trigger an agentic loop, click an iteration in the sidebar. Verify the detail header shows small rounded chips with duration (e.g. "1.2s") and token count (e.g. "3.4k tok") after the iteration name.</manual>
  </verify>
  <done>IterationDetail header shows duration and token count chips using shared format_metrics utilities. Monochrome chip styling via .ai-metric-chip class in app.scss. Errored iterations without tokens show duration chip only (no token chip). Iterations with no duration_ms show no duration chip.</done>
</task>

</tasks>

<verification>
1. `format_metrics.js` exists and exports `formatTokens` and `formatDuration`
2. Sidebar trace rows show metrics line below agent/model line, formatted as "1.2s · 1.2k→2.2k tok"
3. Metrics line updates live as iterations arrive (OWL reactivity through getTraceTotals)
4. Pre-Phase-17 traces with zero data show NO metrics line
5. IterationDetail header shows duration and token chips
6. Errored iterations show duration chip only (no token chip when tokens absent)
7. All SCSS uses Odoo variables only — no hardcoded hex
</verification>

<success_criteria>
- SIDE-01 satisfied: sidebar trace rows show compact metrics line
- DETL-01 satisfied: IterationDetail header shows duration/token chips
- formatTokens and formatDuration are reusable by Plan 02 (LoopDetail)
- No regressions to existing iteration duration display in sidebar tree
</success_criteria>

<output>
After completion, create `.planning/phases/18-display-components-and-animation/18-01-SUMMARY.md`
</output>
