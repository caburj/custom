---
phase: 06-sidebar-tree
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: [ai_debug/static/src/app/app.xml]
autonomous: true
gap_closure: true
requirements: [SIDE-01, SIDE-02]

must_haves:
  truths:
    - "Newest loops appear at the top of the sidebar tree (reverse chronological order)"
    - "Iterations within each loop still display latest-on-top (existing .reverse() unchanged)"
    - "Auto-scroll, flash, selection, expand/collapse all still work after trace order reversal"
  artifacts:
    - path: "ai_debug/static/src/app/app.xml"
      provides: "Reverse chronological trace ordering in sidebar tree"
      contains: "[...traces.keys()].reverse()"
  key_links:
    - from: "ai_debug/static/src/app/app.xml line 37"
      to: "this.traces (useState Map)"
      via: "t-foreach spread with .reverse()"
      pattern: "traces\\.keys\\(\\)\\]\\.reverse\\(\\)"
---

<objective>
Reverse trace rendering order in the sidebar tree so newest loops appear at the top.

Purpose: UAT feedback requests newest-first ordering for traces, matching the existing reverse chronological order already used for iterations. Currently `[...traces.keys()]` iterates in Map insertion order (oldest first).

Output: Single template change — `[...traces.keys()].reverse()` on the trace t-foreach loop.
</objective>

<execution_context>
@/Users/joseph/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joseph/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-sidebar-tree/06-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reverse trace rendering order to newest-first</name>
  <files>ai_debug/static/src/app/app.xml</files>
  <action>
On line 37 of app.xml, change:

```xml
<t t-foreach="[...traces.keys()]" t-as="traceId" t-key="traceId">
```

to:

```xml
<t t-foreach="[...traces.keys()].reverse()" t-as="traceId" t-key="traceId">
```

This matches the existing pattern on line 64 where iterations already use `.reverse()` for latest-on-top ordering.

Do NOT change anything else in the file. The iteration `.reverse()` on line 64, tool call ordering, selection logic, expand/collapse, and all other template logic must remain unchanged.

The auto-scroll behavior in app.js (`scrollIntoView({ block: "nearest" })`) will naturally scroll to the top for new traces since the newest item renders first — this is correct and requires no change.
  </action>
  <verify>
Open ai_debug/static/src/app/app.xml and confirm:
1. Line 37 (the trace t-foreach) contains `[...traces.keys()].reverse()`
2. Line 64 (the iteration t-foreach) still contains `[...trace.iterations.keys()].reverse()`
3. No other lines were changed

Run: `grep -n "traces.keys()" ai_debug/static/src/app/app.xml` — should show `.reverse()` on the trace foreach line.
  </verify>
  <done>The trace t-foreach on line 37 uses `[...traces.keys()].reverse()` so newest loops render at the top of the sidebar tree.</done>
</task>

</tasks>

<verification>
- `grep "traces.keys.*reverse" ai_debug/static/src/app/app.xml` returns the trace foreach line
- `grep "iterations.keys.*reverse" ai_debug/static/src/app/app.xml` returns the iteration foreach line (unchanged)
- No other files modified
- Template is valid XML (no syntax errors)
</verification>

<success_criteria>
Newest agentic loops appear at the top of the sidebar tree. Triggering a second loop places it above the first. Existing iteration reverse ordering is preserved. Selection, expand/collapse, flash, and auto-scroll all continue working.
</success_criteria>

<output>
After completion, create `.planning/phases/06-sidebar-tree/06-05-SUMMARY.md`
</output>
