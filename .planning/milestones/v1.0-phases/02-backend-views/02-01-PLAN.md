---
phase: 02-backend-views
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai_debug/models/ai_debug_trace.py
  - ai_debug/models/ai_debug_iteration.py
  - ai_debug/models/ai_debug_tool_call.py
  - ai_debug/views/ai_debug_trace_views.xml
  - ai_debug/views/ai_debug_iteration_views.xml
  - ai_debug/views/ai_debug_tool_call_views.xml
  - ai_debug/views/menus.xml
  - ai_debug/__manifest__.py
autonomous: true
requirements: [VIEW-01, VIEW-02, VIEW-03, VIEW-04]

must_haves:
  truths:
    - "Developer can open Odoo backend, navigate to Settings > Technical > AI Debug > Traces, and see a list of all trace records"
    - "Trace list shows agent, model, state (colored badge), iteration count, duration (human-readable), and date columns"
    - "Clicking a trace opens a form with notebook tabs: Iterations (One2many summary), System Prompt & RAG, Error Details (visible only when state=error)"
    - "Clicking an iteration row in the trace form opens an iteration form with tabs for messages sent, raw response, state snapshots, and tool calls"
    - "Clicking a tool call row in the iteration form opens a tool call form showing args (ace/JSON), result (plain text), confirmation info, and timing"
    - "Trace list is filterable by agent, model, state, and error_message; has preset 'Errors' and 'Today' filters; supports group-by agent, model, state"
    - "Tool call list has its own search view with tool_name filter"
  artifacts:
    - path: "ai_debug/models/ai_debug_trace.py"
      provides: "duration_human computed Char field"
      contains: "_compute_duration_human"
    - path: "ai_debug/models/ai_debug_iteration.py"
      provides: "duration_human, tool_call_count, pretty-print Text fields for JSON display"
      contains: "_compute_duration_human"
    - path: "ai_debug/models/ai_debug_tool_call.py"
      provides: "duration_human, pretty-print Text fields for JSON display"
      contains: "_compute_duration_human"
    - path: "ai_debug/views/ai_debug_trace_views.xml"
      provides: "list + form + search views for ai.debug.trace"
      contains: "view_ai_debug_trace_list"
    - path: "ai_debug/views/ai_debug_iteration_views.xml"
      provides: "form + search views for ai.debug.iteration"
      contains: "view_ai_debug_iteration_form"
    - path: "ai_debug/views/ai_debug_tool_call_views.xml"
      provides: "form + search views for ai.debug.tool.call"
      contains: "view_ai_debug_tool_call_form"
    - path: "ai_debug/views/menus.xml"
      provides: "ir.actions.act_window + menu items for all 3 models"
      contains: "action_ai_debug_trace"
    - path: "ai_debug/__manifest__.py"
      provides: "Updated data list referencing all view XML files"
      contains: "views/ai_debug_trace_views.xml"
  key_links:
    - from: "ai_debug/views/ai_debug_trace_views.xml"
      to: "ai_debug/models/ai_debug_trace.py"
      via: "field references in list/form/search XML match model field names"
      pattern: "duration_human|iteration_count|agent_id|llm_model|state"
    - from: "ai_debug/views/ai_debug_iteration_views.xml"
      to: "ai_debug/models/ai_debug_iteration.py"
      via: "ace widget on computed Text fields, not raw Json fields"
      pattern: "messages_sent_pretty.*widget.*ace"
    - from: "ai_debug/views/menus.xml"
      to: "ai_debug/views/ai_debug_trace_views.xml"
      via: "action res_model references model name; menu action references action XML ID"
      pattern: "action_ai_debug_trace"
    - from: "ai_debug/__manifest__.py"
      to: "ai_debug/views/*.xml"
      via: "data list entries load XML files on module install/upgrade"
      pattern: "views/.*\\.xml"
---

<objective>
Create standard Odoo backend views (list, form, search) for the three debug models so captured trace data is browsable and filterable without touching any code.

Purpose: Make Phase 1's captured agentic loop data immediately queryable from the Odoo backend. Developers can browse traces, drill into iterations and tool calls, filter by agent/model/state/date, and inspect full JSON payloads in a syntax-highlighted editor.

Output: 4 new XML view files, 3 updated Python model files (computed display fields), updated manifest. All views accessible via Settings > Technical > AI Debug menu.
</objective>

<execution_context>
@/Users/joseph/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joseph/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-views/02-CONTEXT.md
@.planning/phases/02-backend-views/02-RESEARCH.md
@.planning/phases/01-data-models-and-instrumentation/01-01-SUMMARY.md
@ai_debug/models/ai_debug_trace.py
@ai_debug/models/ai_debug_iteration.py
@ai_debug/models/ai_debug_tool_call.py
@ai_debug/__manifest__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add computed display fields to all three debug models</name>
  <files>
    ai_debug/models/ai_debug_trace.py
    ai_debug/models/ai_debug_iteration.py
    ai_debug/models/ai_debug_tool_call.py
  </files>
  <action>
Add computed fields that the XML views will reference. These are display-only fields — no storage, no inverse. Import `json` at the top of iteration and tool_call files.

**ai_debug_trace.py** — Add ONE computed field:
- `duration_human = fields.Char(string='Duration', compute='_compute_duration_human')` — formats `total_duration_ms` into "42ms", "1.2s", or "3m 42s"
- `_compute_duration_human` method: `@api.depends('total_duration_ms')`, loop over self, format ms < 1000 as "{ms}ms", ms < 60000 as "{ms/1000:.1f}s", else "{min}m {sec}s"

**ai_debug_iteration.py** — Add SIX computed fields + `import json`:
- `duration_human = fields.Char(compute='_compute_duration_human')` — same ms formatting pattern as trace
- `tool_call_count = fields.Integer(compute='_compute_tool_call_count')` — `len(self.tool_call_ids)` (simple len, not _read_group — small dataset with 7-day retention)
- `messages_sent_pretty = fields.Text(compute='_compute_messages_sent_pretty')` — `json.dumps(record.messages_sent, indent=2, ensure_ascii=False) if record.messages_sent else ''`
- `raw_response_pretty = fields.Text(compute='_compute_raw_response_pretty')` — same pattern on raw_response
- `state_before_pretty = fields.Text(compute='_compute_state_pretty')` — json.dumps on state_before
- `state_after_pretty = fields.Text(compute='_compute_state_pretty')` — json.dumps on state_after (both in same compute method, depends on both fields)

Each compute method: `@api.depends('{source_field}')`, loop `for record in self:`, assign computed value.

**ai_debug_tool_call.py** — Add FOUR computed fields + `import json`:
- `duration_human = fields.Char(compute='_compute_duration_human')` — same ms formatting
- `args_pretty = fields.Text(compute='_compute_args_pretty')` — json.dumps on args Json field
- `state_before_pretty = fields.Text(compute='_compute_state_pretty')` — json.dumps on state_before
- `state_after_pretty = fields.Text(compute='_compute_state_pretty')` — json.dumps on state_after (same compute)

Add `from odoo import api, fields, models` to iteration and tool_call files (they currently only import `fields, models` — need `api` for `@api.depends`).
  </action>
  <verify>
Run `python -c "import ast; ast.parse(open('ai_debug/models/ai_debug_trace.py').read()); ast.parse(open('ai_debug/models/ai_debug_iteration.py').read()); ast.parse(open('ai_debug/models/ai_debug_tool_call.py').read()); print('All 3 model files parse OK')"` — must print success with no SyntaxError.

Verify each new field is defined: `grep -c 'duration_human\|_pretty\|tool_call_count' ai_debug/models/ai_debug_*.py` — trace.py should show 2 hits (field + compute), iteration.py ~12 hits, tool_call.py ~8 hits.
  </verify>
  <done>
All three model files have computed display fields: duration_human on each, pretty-print Text fields for every Json field, tool_call_count on iteration. All files parse without syntax errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create XML views, actions, menus, and update manifest</name>
  <files>
    ai_debug/views/ai_debug_trace_views.xml
    ai_debug/views/ai_debug_iteration_views.xml
    ai_debug/views/ai_debug_tool_call_views.xml
    ai_debug/views/menus.xml
    ai_debug/__manifest__.py
  </files>
  <action>
Create `ai_debug/views/` directory. Create 4 XML files and update manifest.

**ai_debug_trace_views.xml** — 3 views:

1. **List view** (`view_ai_debug_trace_list`): `<list string="AI Debug Traces">` with these columns:
   - `agent_id`
   - `llm_model` (string="Model")
   - `state` with `widget="badge"` and decorations: `decoration-info="state == 'running'"`, `decoration-success="state == 'done'"`, `decoration-danger="state == 'error'"`, `decoration-warning="state == 'paused'"`
   - `iteration_count` (string="Iterations")
   - `duration_human` (string="Duration")
   - `create_date` (string="Date", `optional="hide"`)
   That is 6 columns per locked decision. `create_date` uses `optional="hide"` (visible by default but user can toggle).

2. **Form view** (`view_ai_debug_trace_form`): `<form><sheet>` with:
   - Two `<group>` columns at top: Left group has agent_id, llm_model, state (badge widget with same decorations), termination_reason — all `readonly="1"`. Right group has start_time, duration_human, iteration_count — all `readonly="1"`.
   - `<notebook>` with 3 pages:
     - Page "Iterations": `<field name="iteration_ids" nolabel="1" readonly="1">` with embedded `<list>` showing: index (string="#"), duration_human (string="Duration"), tool_call_count (string="Tool Calls")
     - Page "System Prompt &amp; RAG": fields `instructions` and `rag_context`, both `readonly="1"`, using default Text widget
     - Page "Error Details" with `invisible="state != 'error'"`: field `error_message` `readonly="1"`

3. **Search view** (`view_ai_debug_trace_search`): `<search string="AI Debug Traces">` with:
   - `<field name="agent_id"/>`, `<field name="llm_model" string="Model"/>`, `<field name="state"/>`, `<field name="error_message"/>`
   - `<separator/>`
   - `<filter string="Errors" name="errors" domain="[('state', '=', 'error')]"/>`
   - `<filter string="Today" name="today" domain="[('create_date', '&gt;=', 'today')]"/>`
   - `<separator/>`
   - `<group expand="0" string="Group By">` with filters for agent_id, llm_model, state using `context="{'group_by': '...'}"` pattern

**ai_debug_iteration_views.xml** — 2 views:

1. **Form view** (`view_ai_debug_iteration_form`): `<form><sheet>` with:
   - Top `<group><group>` pair: Left: trace_id, index — readonly. Right: duration_human — readonly.
   - `<notebook>` with 4 pages per locked decision:
     - "Messages Sent": `messages_sent_pretty` with `widget="ace"` `options="{'mode': 'json'}"` `readonly="1"` `nolabel="1"`
     - "Raw Response": `raw_response_pretty` same ace pattern
     - "State Snapshots": `state_before_pretty` and `state_after_pretty` each with ace widget, with `<separator string="State Before"/>` and `<separator string="State After"/>` labels
     - "Tool Calls": `<field name="tool_call_ids" nolabel="1" readonly="1">` with embedded `<list>` showing: tool_name, success, duration_human, triggered_confirmation

2. **Search view** (`view_ai_debug_iteration_search`): Basic search with `<field name="trace_id"/>` and `<field name="index"/>`

**ai_debug_tool_call_views.xml** — 2 views:

1. **Form view** (`view_ai_debug_tool_call_form`): `<form><sheet>` with:
   - Top `<group><group>` pair: Left: iteration_id, tool_name, call_id — readonly. Right: success, duration_human, triggered_confirmation — readonly.
   - `<notebook>`:
     - "Arguments": `args_pretty` with `widget="ace"` `options="{'mode': 'json'}"` `readonly="1"` `nolabel="1"`
     - "Result": `result` field with `readonly="1"` `nolabel="1"` (plain text widget, not ace — per locked decision: "result uses plain text widget")
     - "Confirmation" with `invisible="not triggered_confirmation"`: `confirmation_message` `readonly="1"`
     - "State Snapshots": `state_before_pretty` and `state_after_pretty` with ace widget, separated by `<separator>` labels

2. **Search view** (`view_ai_debug_tool_call_search`): `<field name="tool_name"/>`, `<field name="iteration_id"/>`, with filter for `<filter string="Confirmations" name="confirmations" domain="[('triggered_confirmation', '=', True)]"/>`

**menus.xml** — 3 actions + 4 menu items:

Actions (`ir.actions.act_window`):
- `action_ai_debug_trace`: res_model=ai.debug.trace, view_mode=list,form, context with `search_default_today: 1` (per research pattern 8)
- `action_ai_debug_iteration`: res_model=ai.debug.iteration, view_mode=list,form
- `action_ai_debug_tool_call`: res_model=ai.debug.tool.call, view_mode=list,form

Menu items:
- `menu_ai_debug_root`: name="AI Debug", parent="base.menu_action_technical", sequence="100"
- `menu_ai_debug_traces`: name="Traces", parent=root, action=trace action, sequence="10"
- `menu_ai_debug_iterations`: name="Iterations", parent=root, action=iteration action, sequence="20"
- `menu_ai_debug_tool_calls`: name="Tool Calls", parent=root, action=tool_call action, sequence="30"

**__manifest__.py** — Update `data` list to include view files. Order: security CSV first, then view files, menus.xml last (menus reference actions defined in view files — but actions are in menus.xml, so actually: security, views, menus):

```python
'data': [
    'security/ir.model.access.csv',
    'views/ai_debug_trace_views.xml',
    'views/ai_debug_iteration_views.xml',
    'views/ai_debug_tool_call_views.xml',
    'views/menus.xml',
],
```

All XML files must use `<?xml version="1.0" encoding="utf-8"?>` header and wrap content in `<odoo>` root element. Use Odoo 17 syntax throughout: `<list>` not `<tree>`, `invisible="expr"` not `attrs`, `readonly="1"` on computed/display fields.
  </action>
  <verify>
1. Validate XML syntax: `python -c "from xml.etree.ElementTree import parse; [parse(f'ai_debug/views/{f}') for f in ['ai_debug_trace_views.xml', 'ai_debug_iteration_views.xml', 'ai_debug_tool_call_views.xml', 'menus.xml']]; print('All XML valid')"` — must print success.
2. Verify manifest data list: `grep -c 'views/' ai_debug/__manifest__.py` — should show 4 (four view file references).
3. Verify badge widget: `grep 'widget="badge"' ai_debug/views/ai_debug_trace_views.xml` — should appear in both list and form views.
4. Verify ace widget: `grep 'widget="ace"' ai_debug/views/ai_debug_iteration_views.xml ai_debug/views/ai_debug_tool_call_views.xml` — should show hits in both files.
5. Verify search filters: `grep '<filter' ai_debug/views/ai_debug_trace_views.xml` — should show Errors, Today, and 3 group-by filters.
6. Verify menus: `grep 'menuitem' ai_debug/views/menus.xml` — should show 4 menu items.
  </verify>
  <done>
Four XML view files created with complete list/form/search views for all three models. Menu structure under Settings > Technical > AI Debug with Traces, Iterations, and Tool Calls entries. Manifest updated to load all view files. All views use Odoo 17 syntax, ace widget for JSON display, badge widget for state.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. All 3 Python model files parse without syntax errors
2. All 4 XML files are well-formed XML
3. Manifest references all 4 XML files in the data list
4. Trace list view has exactly 6 columns per locked decision
5. Trace form has 3 notebook tabs (Iterations, System Prompt & RAG, Error Details)
6. Error Details tab has `invisible="state != 'error'"`
7. Iteration form has 4 notebook tabs (Messages Sent, Raw Response, State Snapshots, Tool Calls)
8. All JSON fields use computed Text pretty-print fields with ace widget — NOT raw Json fields
9. Search view has agent, model, state, error_message fields + Errors/Today filters + 3 group-by options
10. Tool call search view has tool_name filter
</verification>

<success_criteria>
- Module can be installed/upgraded in Odoo without XML parse errors or missing field errors
- Developer navigates to Settings > Technical > AI Debug > Traces and sees trace list
- Trace list shows colored state badges, human-readable duration, correct columns
- Clicking a trace opens form with tabbed iterations, prompt, and conditional error tab
- Iteration drill-down shows JSON payloads in syntax-highlighted ace editor
- Search bar supports filtering by agent, model, state; preset Errors/Today filters work
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-views/02-01-SUMMARY.md`
</output>
